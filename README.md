#	Issue	Symptom	Affected File(s)	Root Cause	Fix Required
S-01	Stream function is NOT a generator	generator=False in log; _execute_function called instead of _execute_stream_function; CAN bus opened but no data yielded	runner.py _run_with_retries(), Read_Photosensor.py (test function)	is_generator = _is_generator_function(fn) returns False because the test function uses return not yield. is_stream = mode.is_streaming or is_generator — mode is stream so is_stream=True, but is_generator=False, so exec_fn = _execute_function is used, NOT _execute_stream_function	Option A (Preferred): Rewrite all stream auto-run .py functions to use yield instead of return. Option B: In _run_with_retries, if mode.is_streaming is True regardless of is_generator, always use _execute_stream_function. Add wrapper that converts non-generator stream functions to generators
S-02	CAN bus opened per-program, not shared	PcanCanInitializationError: A PCAN Channel has not been initialised yet when second stream starts	can_utils.py, all auto-run .py modules, service.py start_auto_run()	Each auto-run program calls open_can_bus() independently. PCAN hardware only allows one Bus instance per channel at a time. When program 1 holds the bus and program 2 tries to open it, it fails	Create a shared CAN bus manager in can_utils.py. Add get_or_open_can_bus(channel, bitrate) that returns an existing bus instance if already open. Auto-run programs should call this instead of open_can_bus(). Reference-count active users and only close when count reaches 0
S-03	CAN bus not closed when session ends	Stream programs keep CAN bus open after navigation or session close	runner.py stop_auto_run_session(), service.py stop_auto_run(), auto-run .py modules	stop_auto_run_session() calls task.cancel() which sets the cancel event, but if the test function does not call context.checkpoint() regularly (e.g., blocked in bus.recv()), the cancel event is never seen. CAN bus shutdown() is never called	(a) Auto-run stream functions must call context.checkpoint() inside their recv loop. (b) Add a finally: bus.shutdown() block in every stream function. (c) With shared bus manager (S-02 fix), call release_can_bus(channel) in finally — bus only shuts down when all users released
S-04	Stream values not shown in UI	Battery voltage stream runs (task status = RUNNING) but no values appear in the auto-run display panel	service.py on_stream_data_wrapper(), _persist_stream_value(), get_auto_run_status(), frontend JavaScript	Multiple sub-causes: (a) If S-01 not fixed, no data is ever yielded so nothing to display. (b) _persist_stream_value writes to app.auto_run_stream_values but UI polls get_auto_run_status() which reads this table — if polling interval is too slow or JS is not rendering the display_values.{pid}.{signal} keys, values appear blank. (c) display_values key format is f"{row['program_id']}.{row['signal_name']}" — frontend must use same key format	(a) Fix S-01 first. (b) Add a dedicated /api/auto_run/<session_id>/stream_values polling in the frontend JS with 500ms interval. (c) Verify frontend renders keys matching program_id.signal_name format. (d) Add progress_json emission inside the generator yield loop so values also appear in task logs immediately without DB round-trip
S-05	ECU page auto-run (ecu_active_check) never starts	ECU active status dots never appear; no tasks created for ECU scope	Website_With_DB.py tests_root(), service.py start_auto_run(), frontend JavaScript	When the ECU page loads (Phase 2 in tests_root), the frontend does NOT automatically call POST /api/run_auto_programs with scope=ecu:ECU_CODE. The web route only renders the template — no auto-run trigger exists for ECU-scope sessions. get_active_auto_run_session() also only resumes if user_login_id and DB row exist, but no row was ever created	(a) In the frontend JS, when the ECU page loads (URL has ?section=diagnostics&ecu=ECU_CODE but no parameter), automatically call POST /api/run_auto_programs with {"vehicle_name": ..., "section": "diagnostics", "scope": "ecu:ECU_CODE"}. (b) In Website_With_DB.py api_run_auto_programs(), ensure SESSION_SCOPE_ECU_PFX scope is correctly handled. (c) start_auto_run() already handles ECU scope correctly — the missing piece is the frontend trigger
S-06	Streams not stopped on page navigation	Battery voltage keeps streaming when user moves from section page to ECU page	Frontend JavaScript, Website_With_DB.py api_auto_run_stop()	No beforeunload or visibilitychange listener calls POST /api/auto_run/<session_id>/stop when navigating between pages. The api_session_end endpoint only fires on browser close, not page navigation	Add a page_scope concept: in frontend JS, on every page navigation (URL change), call POST /api/auto_run/<prev_session_id>/stop for the previous page's session before starting the new page's session. Use history.pushState interception or window.addEventListener('popstate', ...)
S-07	Streams not restarted on return navigation	After navigating away and back, stream does not restart	Frontend JavaScript, service.py get_active_auto_run_session()	get_active_auto_run_session() resumes if last_accessed > NOW() - INTERVAL '5 minutes' AND status is running/started. But if S-06 fix stops the session on navigate-away, status becomes stopped — not resumable. Need to distinguish "user navigated away temporarily" from "user finished"	(a) Instead of stop, use pause semantics on navigate-away: mark session as paused in DB. (b) On return, if session is paused within 10 minutes, resume it by calling start_auto_run_session() again with the same session_id. (c) Alternatively: always create a fresh session on return (simpler, no pause logic) — current session cleanup handles old ones within 2 minutes
S-08	Auto-run and test sequence share CAN bus unsafely	Running test sequence while auto-run stream is active causes CAN bus contention	runner.py _run_with_retries() ISSUE-07 comment, can_utils.py	Both auto-run tasks and user-triggered test tasks call open_can_bus() independently. PCAN channel is exclusive. Test sequence task opening bus while stream holds it → PcanCanInitializationError	With shared bus manager (S-02 fix), all callers share one bus instance safely. Additionally, in can_utils.py, implement a per-channel lock so only one task sends/receives at a time on the same channel. The stream program should use a separate receive thread pattern rather than blocking the task thread
S-09	Auto-run threads block main request thread	When auto-run programs are slow, Flask request handling is delayed	Website_With_DB.py api_run_auto_programs(), service.py start_auto_run()	api_run_auto_programs() calls start_auto_run() which calls start_auto_run_session() which starts a background thread — this is already async. However, run_auto_programs() legacy wrapper BLOCKS: it polls get_auto_run_status() in a while loop on the Flask worker thread	Always use start_auto_run() (non-blocking). Never call legacy run_auto_programs(). Verify api_run_auto_programs in Website_With_DB.py calls start_auto_run() not run_auto_programs(). Add DeprecationWarning log in run_auto_programs() to detect if it is still being called
S-10	generator=False causes wrong executor selection	Log shows generator=False even when execution_mode=stream — single-shot executor is used	runner.py _run_with_retries() lines: is_generator = _is_generator_function(fn) and exec_fn = _execute_stream_function if (is_stream and is_generator) else _execute_function	The condition is_stream and is_generator is True and False = False when mode is stream but function is not a generator. This silently falls back to single-shot execution with no error until the function fails	Change the condition: exec_fn = _execute_stream_function if is_stream else _execute_function. Separately validate that if is_stream=True and is_generator=False, either: raise error immediately, or wrap function in a generator adapter. Add explicit log: _log_error("Stream mode requested but function is not a generator")
S-11	Run-All with mixed stream/single does not isolate streams	Clicking "Run All" in a parameter with both stream and single tests causes streams to conflict	service.py run_all_tests_for_parameter(), runner.py BatchExecutor	run_all_tests_for_parameter() passes all tests to execute_batch_async() without separating streams from singles. In sequential mode, a stream test blocks the batch forever (streams have no timeout). In parallel mode, multiple streams compete for the CAN bus	(a) In run_all_tests_for_parameter(), separate stream tests from single tests. Run singles in the batch. Start streams as independent auto-run-style sessions. (b) OR: in BatchExecutor._execute_sequential(), detect stream tasks and set a reasonable time limit (e.g., 30 seconds) before moving to the next test
S-12	Stream program cleanup missing from cleanup_loop	Stale stream tasks accumulate in _TASK_REGISTRY indefinitely	Website_With_DB.py cleanup_loop(), runner.py TaskRegistry.cleanup_completed()	cleanup_completed() removes terminal tasks older than TASK_RETENTION_SEC (1 hour). Stream tasks in RUNNING state (never terminal because stream never ends) are NEVER cleaned up. After 1 hour, task count grows unboundedly	Add a stream-specific cleanup: if a task is RUNNING, mode is stream, AND last_accessed (or started_at) is older than AUTO_RUN_SESSION_MAX_AGE_SEC (2 hours), cancel and remove it. Add this check to _start_cleanup_thread() in Website_With_DB.py
S-13	display_values key collision between programs	If two stream programs emit the same signal name, they overwrite each other in display_values	service.py get_auto_run_status()	In get_auto_run_status(), stream values are keyed as f"{program_id}.{signal_name}". But when building display_values, single-shot values are keyed as just pid. If pid happens to equal program_id.signal_name there could be a collision	Standardize: stream values keyed as stream:{program_id}:{signal_name}, single values as single:{program_id}. Update frontend JS to handle these prefixed keys
S-14	vin_triggered set not thread-safe	VIN fallback could trigger on_vin_needed multiple times under concurrent callbacks	service.py start_auto_run() closure on_single_result_wrapper()	vin_triggered: Set[str] is a plain set() defined in outer scope and mutated from the callback thread (which is a background thread). If two results arrive simultaneously, both could see pid not in vin_triggered before either adds it	Replace vin_triggered: Set[str] with threading.Lock() + set, or use threading.Event per program_id
S-15	single_completed race condition	Session marked complete before all singles actually finish	service.py start_auto_run() closure on_single_result_wrapper()	single_completed_lock protects the set, but should_complete is computed inside the lock and then acted on outside: python with single_completed_lock: single_completed.add(pid) should_complete = (single_completed >= single_program_ids) if should_complete: _mark_auto_run_session_completed(sid) — _mark_auto_run_session_completed is called outside the lock. A second thread could also compute should_complete=True and double-call	Move _mark_auto_run_session_completed(sid) inside the with single_completed_lock: block, or use a threading.Event that can only be set once
S-16	_AUTO_RUN_SESSIONS accessed directly from service.py	Tight coupling; bypasses public API	service.py submit_auto_run_vin(), runner.py	from diagnostics.runner import _AUTO_RUN_SESSIONS, _AUTO_RUN_LOCK — private variables imported directly. Already noted as a concern in runner.py ISSUE-17	Extend runner.py public API: add update_session_result(session_id, program_id, **kwargs) and update_session_vin(session_id, vin, source) functions. Remove direct _AUTO_RUN_SESSIONS import from service.py
S-17	No mechanism to detect page visibility change	Stream runs even when browser tab is hidden/background	Frontend JavaScript	No document.addEventListener('visibilitychange', ...) handler exists to pause/stop streams when tab becomes hidden	Add visibilitychange listener: on hidden → call heartbeat stop or reduce polling; on visible → resume polling and restart streams if needed
S-18	get_active_auto_run_session() has unreferenced variable in_memory	NameError or wrong variable used in session resume check	service.py get_active_auto_run_session()	In the loop over rows, the code checks if in_memory is None: and mem_status = in_memory.get("status", "") — but in_memory is never defined in that scope. It should be in_memory = get_auto_run_session(row["session_id"]) before the check	Add in_memory = get_auto_run_session(row["session_id"]) before if in_memory is None:
S-19	Auto-run stream programs share ExecutionContext incorrectly	ctx.stream_callback overwritten per-spec in loop, but same ctx object may be referenced	service.py start_auto_run()	In the stream callback setup loop: spec.ctx.stream_callback = _make_stream_cb(spec.program_id) — this mutates the ctx object on the spec. If ExecutionContext is shared between retries (it is — same ctx passed to _run_with_retries), and the runner also sets ctx.stream_callback = original_stream_callback on retry (FIX-51), there could be a double-assignment conflict	Each spec already has its own ExecutionContext created in the loop — this is safe. However, verify that create_result_persister() does not close over a shared vehicle_id that changes in loop iteration (it does not — each call creates a new closure). No fix needed here, but add a comment confirming isolation
S-20	No frontend session_id management across page navigations	Frontend loses track of session_id when navigating between pages	Frontend JavaScript, Website_With_DB.py tests_root()	When user navigates Section → ECU → Parameter, each page load creates a new auto_run_session_id URL parameter (if passed). But if the frontend does not pass the section's session_id forward AND stop the section's streams, both sessions run simultaneously	(a) Pass auto_run_session_id as URL parameter through all page navigations. (b) When starting a new scope session, always stop the previous scope's session first. (c) Store session_id per scope in sessionStorage keyed by scope: sessionStorage.setItem('auto_run_section', session_id)
